# Domain documentation

This is one ReStock domain context, despite separate frontend, backend, and agent packages.

- Read CONTEXT.md for domain terminology before relevant work.
- Read relevant docs/adr entries if they exist; proceed without creating empty ADR scaffolding if absent.
- Use glossary terms in specifications, tickets, API explanations, and implementation notes.
- Report conflicts with a relevant ADR instead of silently overriding it.
- Architecture and backend specification documents describe behaviour and shared contracts; CONTEXT.md remains a glossary, not an implementation scratchpad.
